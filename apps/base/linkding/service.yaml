apiVersion: v1
kind: Service
metadata:
  name: linkding
  labels:
    app.kubernetes.io/name: linkding
    app.kubernetes.io/instance: linkding
    app.kubernetes.io/part-of: homelab
spec:
  ports:
  - port: 9090
  selector:
    app: linkding
  type: ClusterIP
