apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mealie-cert
  namespace: mealie
spec:
  secretName: mealie-tls
  issuerRef:
    name: internal-ca
    kind: ClusterIssuer
  dnsNames:
    - mealie.jellylabs.xyz
