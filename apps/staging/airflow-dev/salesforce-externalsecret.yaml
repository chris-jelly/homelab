apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: salesforce-credentials
  namespace: airflow-dev
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-keyvault
    kind: SecretStore
  target:
    name: salesforce-credentials
    creationPolicy: Owner
  data:
  - secretKey: SALESFORCE_USERNAME
    remoteRef:
      key: salesforce-username-dev
  - secretKey: SALESFORCE_PASSWORD
    remoteRef:
      key: salesforce-password-dev
  - secretKey: SALESFORCE_SECURITY_TOKEN
    remoteRef:
      key: salesforce-security-token-dev
  - secretKey: SALESFORCE_DOMAIN
    remoteRef:
      key: salesforce-domain-dev